describe("This is my second protractor test", function() {

	it("This application will add two numbers", function() {
		
		
		browser.get("http://gmail.com");
		element(by.model("")).sendKeys("");
		element(by.model("")).sendKeys("");
		element(by.id("")).click();
		
		expect(element(by.className(""))).getText().toEqual("");
		browser.
		
	})
	
})
